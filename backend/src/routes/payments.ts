import {Router} from 'express';import crypto from 'crypto';import Razorpay from 'razorpay';import {db} from '../lib/db';
const r=Router();
r.post('/create-order',async(req,res)=>{if(!process.env.RAZORPAY_KEY_ID||!process.env.RAZORPAY_KEY_SECRET)return res.status(503).json({message:'Razorpay is not configured'});const rp=new Razorpay({key_id:process.env.RAZORPAY_KEY_ID,key_secret:process.env.RAZORPAY_KEY_SECRET});const amount=Math.round(Number(req.body.amount)*100);const order=await rp.orders.create({amount,currency:'INR',receipt:`DRV-${Date.now()}`,payment_capture:true});res.json(order)});
r.post('/verify',async(req,res)=>{const {razorpay_order_id,razorpay_payment_id,razorpay_signature}=req.body;const expected=crypto.createHmac('sha256',process.env.RAZORPAY_KEY_SECRET||'').update(`${razorpay_order_id}|${razorpay_payment_id}`).digest('hex');if(expected!==razorpay_signature)return res.status(400).json({message:'Invalid payment signature'});res.json({verified:true})});
r.post('/webhook',async(req,res)=>{const raw=JSON.stringify(req.body);const sig=req.headers['x-razorpay-signature'];const expected=crypto.createHmac('sha256',process.env.RAZORPAY_WEBHOOK_SECRET||'').update(raw).digest('hex');if(!sig||sig!==expected)return res.status(400).json({message:'Invalid webhook'});res.json({ok:true})});export default r;
